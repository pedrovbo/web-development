var alunos = new Array();

    var alunos = new Array('Alex', 'Anna', 'João');

    for(var i = 0; i < alunos.length; i++){
    console.log('Valores contidos no Array: ' +alunos[i]);
    }

    alunos.push('Gessica', 'Pedro');//método utilizado para incrementar um ou mais índices
    //e respectivos valores em um array

    for(var i = 0; i < alunos.length; i++){
        console.log('Valores contidos no Array: ' +alunos[i]);
        }